import React from "react";
import ReactDOM from "react-dom";//ReactDOM.render es la función que le dice a React qué renderizar y dónde renderizarlo.
import App from "./App.js";
ReactDOM.render(
  <App />,
  document.getElementById("root")
);